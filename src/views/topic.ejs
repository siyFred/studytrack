<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= topic.title %> | StudyTrack</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body style="min-height:100vh;display:flex;flex-direction:column;">
  <header class="header">
    <div class="container header-content">
      <div class="logo">
        <div class="logo-icon"></div>
        <span class="logo-text">StudyTrack</span>
      </div>
      <button class="menu-button" id="menu-button">☰</button>
      <nav class="nav" id="nav">
        <a href="/">Home</a>
        <a href="/dashboard" class="btn-nav">Dashboard</a>
        <a href="/logout" class="btn-nav btn-outline">Sair</a>
      </nav>
    </div>
  </header>
  <section class="hero" style="flex:1;">
    <div class="container" style="max-width: 700px; margin: 0 auto;">
      <h1 style="text-align:center; color:#a78bfa;"><%= topic.title %></h1>
      <p style="text-align:center; color:#d1d5db; margin-bottom:30px; font-size:1.1rem;">
        <%= topic.description %>
      </p>
      <div style="background:#1f2937;padding:30px;border-radius:10px;">
        <h2 style="color:#a78bfa;">Passos</h2>
        <ol style="margin:20px 0 0 0;list-style:decimal inside;padding:0;">
          <% if (topic.steps && topic.steps.length > 0) { %>
            <% topic.steps.forEach(step => { %>
              <li style="background:#232946;padding:15px 20px;border-radius:8px;margin-bottom:15px;">
                <strong><%= step.title %></strong>
                <div style="color:#d1d5db;margin-top:8px;white-space:pre-line;">
                  <%= step.content %>
                </div>
              </li>
            <% }) %>
          <% } else { %>
            <li style="color:#d1d5db;">Nenhum passo cadastrado para este tópico.</li>
          <% } %>
        </ol>
      </div>
    </div>
  </section>
  <footer class="footer">
    <div class="container">
      <p>© 2025 StudyTrack. All rights reserved.</p>
    </div>
  </footer>
  <script>
    const menuButton = document.getElementById('menu-button');
    const nav = document.getElementById('nav');
    menuButton.addEventListener('click', () => {
      nav.classList.toggle('show');
    });
  </script>
</body>
</html>
