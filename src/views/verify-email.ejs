<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Verificar Email | StudyTrack</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header class="header">
    <div class="container header-content">
      <div class="logo">
        <div class="logo-icon"></div>
        <span class="logo-text">StudyTrack</span>
      </div>
      <button class="menu-button" id="menu-button">☰</button>
      <nav class="nav" id="nav">
        <a href="/">Início</a>
        <a href="/login" class="btn-nav">Entrar</a>
      </nav>
    </div>
  </header>
  <section class="hero flex-1">
    <div class="container container-400">
      <h1 class="text-center">Verificação de Email</h1>
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="error-message text-error mb-15 text-center"> <%= error %> </div>
      <% } %>
      <form action="/api/verify-email" method="POST" class="mt-40">
        <input type="hidden" name="email" value="<%= email %>">
        <div class="mb-20">
          <label for="code">Código de verificação</label>
          <input type="text" id="code" name="code" required minlength="6" maxlength="6" class="w-100 p-10 mt-5 rounded-6 b-1 bg-input">
        </div>
        <div class="text-purple text-center mb-10">(MVP: Seu código é <%= code %>)</div>
        <button type="submit" class="btn-primary w-100">Verificar</button>
      </form>
    </div>
  </section>
  <footer class="footer">
    <div class="container">
      <p>© 2025 StudyTrack. All rights reserved.</p>
    </div>
  </footer>
  <script>
    const menuButton = document.getElementById('menu-button');
    const nav = document.getElementById('nav');
    menuButton.addEventListener('click', () => {
      nav.classList.toggle('show');
    });
  </script>
</body>
</html>
