<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Redefinir Senha | StudyTrack</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header class="header">
    <div class="container header-content">
      <div class="logo">
        <div class="logo-icon"></div>
        <span class="logo-text">StudyTrack</span>
      </div>
      <button class="menu-button" id="menu-button">☰</button>
      <nav class="nav" id="nav">
        <a href="/">Home</a>
        <a href="/login">Entrar</a>
        <a href="/register">Cadastrar</a>
      </nav>
    </div>
  </header>
  <section class="hero flex-1">
    <div class="container container-400">
      <h1 class="text-center">Redefinir Senha</h1>
      <form action="/api/reset-password" method="POST" class="mt-40">
        <input type="hidden" name="email" value="<%= email %>">
        <div class="mb-20">
          <label for="code">Código recebido</label>
          <input type="text" id="code" name="code" required maxlength="6" class="w-100 p-10 mt-5 rounded-6 b-1 bg-input">
        </div>
        <div class="mb-20">
          <label for="password">Nova senha</label>
          <input type="password" id="password" name="password" required minlength="8" class="w-100 p-10 mt-5 rounded-6 b-1 bg-input">
        </div>
        <button type="submit" class="btn-primary w-100">Redefinir senha</button>
        <% if (typeof error !== 'undefined' && error) { %>
          <div class="error-message text-error mt-15 text-center"> <%= error %> </div>
        <% } %>
        <% if (typeof message !== 'undefined' && message) { %>
          <div class="success-message text-success mt-15 text-center"> <%= message %> </div>
        <% } %>
      </form>
    </div>
  </section>
  <footer class="footer">
    <div class="container">
      <p>© 2025 StudyTrack. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>
